import { createStyles, makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() =>
  createStyles({
    icon: {
      width: '0.5rem',
      height: '0.5rem',
    },
  })
)
