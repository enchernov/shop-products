import { createStyles, makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(() =>
  createStyles({
    root: {
      padding: 20,
    },
    image: {},
    link: {},
  })
)
